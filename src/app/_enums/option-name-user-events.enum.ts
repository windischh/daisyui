/**
 * option name userEvents - are persisted for each user
 */
export enum OptionNameUserEvents {
   /* isImmediatePersist- if yes, each choice of a user concerning user options 6 - 12
      is immediately stored as new user option
      otherwise user has a function to persist his actual choces */
  isImmediatePersist= 1,
  style = 2,
  timeSlize = 3,
  startHour = 4,
  lastHour = 5,
  }


