/**
 * configuration area enumerates all optionAreas
 *
 */
export enum ConfigurationArea {
  // order and invoice are in pevent-invoice
  // order = 1,
  // invoice = 2,
  events = 3,
  // userEvents and eventCalendar are in pEvent-log
  userEvents = 4,
  eventCalendar = 5,
  // invoicePrint = 6,
  // timeSpan and todo are in pEvent-log
  timeSpanCategory = 7,
  // otdos are not implemented
  // todoCategory = 8,
  eventExport = 9,
  documentCategory = 10
  }

