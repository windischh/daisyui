/**
 * IEventSortCriteria
 * used in event select option
 */
export interface IEventSortCriteria {
  sortField: string;
  isSortCriteria: boolean;
  isSum: boolean;
  isSubTotal: boolean;
}
