/**
 * INumChoice
 * general structure to handle choices
 * with numeric value
 */
export interface INumChoice {
  value: number;
  text: string;
  isSelected?: boolean;
  timeStyle?: {};
}
